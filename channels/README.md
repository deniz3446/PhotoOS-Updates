# PhotoOS Update Channels

No update channel is active merely because this repository exists.

`beta.json` / `beta.json.sig` and `stable.json` / `stable.json.sig` are created only when a genuine PhotoOS application update candidate has passed the release-builder verification and publication flow.

The signed channel manifest is the final publication switch.
