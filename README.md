# PhotoOS Updates

This repository is the public transport location for official signed PhotoOS update artifacts.

Authenticity is established by the pinned PhotoOS Ed25519 public key and detached signatures, not by GitHub alone.

Application source code and the private signing key are not stored here.

Release packages are distributed as GitHub Release assets. Signed channel manifests are published under `channels/` only after the corresponding release assets are fully uploaded and verified.
